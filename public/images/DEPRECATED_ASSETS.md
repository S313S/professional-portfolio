# Deprecated Career Detail Assets

These files are retained only for historical reference and old diff reports. They are not used by the current `CareerDetailSection` runtime.

Deprecated files:
- `careerDetail_map.png`
- `careerDetail_map.svg`

Current replacement:
- `careerDetail_litteleBg.png`

Rule:
- Do not wire `careerDetail_map.png` or `careerDetail_map.svg` back into the aside.
- The current aside uses the full archival card image instead of the older map-only treatment.
