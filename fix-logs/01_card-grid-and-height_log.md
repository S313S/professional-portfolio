1. **理解**：这轮 issue #1 已从“勋章太小”变成“上一轮放得过大”。当前问题集中在右侧勋章图和勋章列宽数值偏大，挤压了文本区，进而让第一条记录更容易越界。
2. **分析**：我重新读取了 `docs/diff-reports/round1_2026-04-16_friendbook-card-layout/report.md` 与最新的 `issues/01_card-grid-and-height.md`，再核对 `src/components/FriendBookFinalSection.tsx` 顶部的 `FRIEND_BOOK_SAMPLE_ENTRY_GRID`。现状是 `outer` 仍为 `120px / 140px`，`medalSize` 仍为 `h-20 w-20` / `xl:h-[7rem] xl:w-[7rem]`，而报告要求已更新为约 `100px / 110px` 的勋章列和约 `4.5rem / 5.8rem` 的勋章尺寸；同时样例卡片上的头像/勋章边框样式也被意外移除了，不符合报告里“保留现有视觉样式”的约束。
3. **方案**：保持 `sampleEntries` 的 `20/45/70` 与 `22%` 高度不变，只回调 issue #1 的数值参数，不碰 issue #2 已确认的标题+徽章并排布局。这样改动最小，也最符合当前报告。
4. **改动**：修改了 `src/components/FriendBookFinalSection.tsx`，将 `FRIEND_BOOK_SAMPLE_ENTRY_GRID.outer` 从 `120/140` 收到 `100/110`，将 `medalSize` 从 `h-20 w-20` / `xl:h-[7rem] xl:w-[7rem]` 收到 `h-[4.5rem] w-[4.5rem]` / `xl:h-[5.8rem] xl:w-[5.8rem]`；同时把样例卡片中的头像与勋章图片边框 `border border-[#d4bea8]` 恢复。同步更新了 `src/components/FriendBookFinalSection.preview-copy.render.test.tsx`，先把断言改成新目标值并运行，确认旧实现失败后再改代码。
5. **验证**：先运行 `node --import tsx --test src/components/FriendBookFinalSection.preview-copy.render.test.tsx`，确认它会因旧的 `120/140` 和 `7rem` 断言不匹配而失败；改完实现后再次运行该测试通过。随后再跑 FriendBook 相关测试做回归。
6. **遗留**：这次仍然是基于 render 结构和报告数值回调，没有做浏览器像素级截图确认。如果下一轮 diff 仍提示勋章 x/y 偏差，优先通过勋章容器的对齐或 padding 继续细调，而不是再次大幅放大勋章列。
7. **可调参数**：已提取。位置仍在 `src/components/FriendBookFinalSection.tsx` 顶部的 `FRIEND_BOOK_SAMPLE_ENTRY_GRID`，其中 `outer.base`、`outer.xl`、`medalSize.base`、`medalSize.xl` 是本轮的主要可调入口；绝对定位仍集中在同文件的 `FRIEND_BOOK_ARCHIVE_DESKTOP_LAYOUT.sampleEntries`。
