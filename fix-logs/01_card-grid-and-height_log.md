1. **理解**：左页 sample entry 的主要问题是外层三列 grid 里右侧勋章列过窄、勋章图过小，同时绝对定位高度过低，导致内容被压缩并出现不同程度的溢出。
2. **分析**：我先读取了 `docs/diff-reports/round1_2026-04-16_friendbook-card-layout/report.md` 和 `issues/01_card-grid-and-height.md`，再检查 `src/components/FriendBookFinalSection.tsx` 的 `FRIEND_BOOK_ARCHIVE_DESKTOP_LAYOUT.sampleEntries` 与桌面 sample entry JSX。现状与报告一致：三张卡高度仍为 `16.4%`，top 位置是 `24.0% / 47.3% / 70.4%`，外层 grid 仍是 `grid-cols-[58px_minmax(0,1fr)_88px]` 与 `xl:grid-cols-[68px_minmax(0,1fr)_98px]`，右侧 medal 图仍是 `h-15 w-15` / `xl:h-[4.4rem] xl:w-[4.4rem]`。
3. **方案**：采用报告给出的结构性修复方案，优先扩大卡片容器高度并重排三张卡的绝对定位，再同步放大右侧勋章列和 medal 图尺寸。为了便于后续继续微调，我把桌面 sample entry 的关键 class 收敛到 `FRIEND_BOOK_SAMPLE_ENTRY_GRID` 配置对象里，避免后续继续分散改多个 JSX 字符串。
4. **改动**：修改了 `src/components/FriendBookFinalSection.tsx`，把 `sampleEntries` 的 top/height 调整为 `20/45/70` 与统一 `22%` 高度；新增 `FRIEND_BOOK_SAMPLE_ENTRY_GRID` 配置；将桌面 sample entry 外层 grid 改成 `..._120px` / `..._140px` 的勋章列；将 medal 图尺寸改为 `h-20 w-20` 与 `xl:h-[7rem] xl:w-[7rem]`。同时更新了 `src/components/FriendBookFinalSection.preview-copy.render.test.tsx`，新增对定位样式、外层 grid 列宽和 medal 图尺寸的 render 断言，并先运行测试确认旧实现会失败。
5. **验证**：先运行 `node --import tsx --test src/components/FriendBookFinalSection.preview-copy.render.test.tsx`，确认在改实现前因旧定位与旧 class 失败；完成修改后再次运行同一测试通过。随后会连同其他 FriendBook 相关测试一起回归。
6. **遗留**：本轮主要通过 render 结构约束确保布局参数落地，没有在浏览器里做像素级截图回归；如果下一轮 diff 仍有轻微纵向间距偏差，优先微调 `sampleEntries` 的 `top` 和 `height`。
7. **可调参数**：已提取。位置在 `src/components/FriendBookFinalSection.tsx` 顶部新增的 `FRIEND_BOOK_SAMPLE_ENTRY_GRID`，其中包含外层列宽、copy/header 布局 class 和 medal 图尺寸；绝对定位参数继续集中在同文件的 `FRIEND_BOOK_ARCHIVE_DESKTOP_LAYOUT.sampleEntries`。
