1. **理解**：这轮问题不是卡片 `rotate(45deg)` 或 slot 步进错了，而是走廊本身太窄。活跃卡片放大后 axis-aligned bounding box 约 `15.8rem`，当前 corridor 只有 `15.5rem`，所以即使中心线正确，也会从上下边界溢出。
2. **分析**：我先读取了 `docs/diff-reports/round3_2026-04-09_card-outside-corridor/report.md` 与 `issues/01_cards-overflow-corridor.md`，再检查 [src/index.css](/Users/xiaoci/Downloads/Workspace/VibeCoding/personal_brand/professional-portfolio/src/index.css) 的 `.works-detail-track__corridor--band`、`--top`、`--bottom`。现状与报告一致：band 高度 `15.5rem`，上下偏移 `7.75rem`，虚线透明度 `0.25`。我还先修改了 [src/components/WorksDetailSection.render.test.tsx](/Users/xiaoci/Downloads/Workspace/VibeCoding/personal_brand/professional-portfolio/src/components/WorksDetailSection.render.test.tsx) 的 CSS 断言并运行测试，确认在改实现前会因为这些旧值失败。
3. **方案**：按报告给出的最小修复方案，只扩大 corridor 和同步边界偏移，不改任何卡片 `translate3d / rotate / scale`。这样可以保留上一轮修复好的 45° 卡片系统，把这轮问题收敛为纯 CSS 容器尺寸问题。
4. **改动**：修改了 [src/index.css](/Users/xiaoci/Downloads/Workspace/VibeCoding/personal_brand/professional-portfolio/src/index.css) 中 corridor 的 band 高度 `15.5rem -> 20rem`、上下边界偏移 `7.75rem -> 10rem`，并把虚线边界从 `rgba(200, 210, 220, 0.25)` 提高到 `rgba(200, 210, 220, 0.4)`；同时更新了 [src/components/WorksDetailSection.render.test.tsx](/Users/xiaoci/Downloads/Workspace/VibeCoding/personal_brand/professional-portfolio/src/components/WorksDetailSection.render.test.tsx) 的回归断言，锁定这组 corridor 值。
5. **验证**：我先运行 `node --import tsx --test src/components/WorksDetailSection.render.test.tsx`，确认新断言在旧 CSS 下失败；改完后再次运行同一命令并通过。随后又运行 `npm run lint` 与 `npm run build`，确认没有引入类型错误或构建回归。
6. **遗留**：这次没有额外引入 corridor 中线偏移，因为报告给出的根因已经能被尺寸不足完全解释。若后续截图仍显示走廊中心与卡片中心线存在轻微错位，再单独微调 `translate(-50%, -50%)` 的 Y 偏移更稳妥。
7. **可调参数**：这轮最关键的可调参数都集中在 [src/index.css](/Users/xiaoci/Downloads/Workspace/VibeCoding/personal_brand/professional-portfolio/src/index.css) 的 `.works-detail-track__corridor*` 规则里，当前主要是 band 高度 `20rem`、上下边界偏移 `10rem` 和虚线透明度 `0.4`。如果还需继续贴近参考图，优先微调这三项即可。
